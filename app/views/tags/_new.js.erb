<% remote_form_for :tagging, @taggable.taggings.new, :url => tags_path_from_taggable(@taggable), :html => { :id => 'tagForm' } do |f| %>
	<%= render :partial => 'form', :locals => { :f => f } %>
<% end %>
<%# add_behavior('tagForm', 'MakeRemoteForm') %>
