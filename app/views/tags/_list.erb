<% content_tag :ul, :class => "tags #{list.class.class_name.underscore}-tags", :id => dom_id(list) + '-tags' do -%>
	<%= render :partial => 'tags/tagging', :collection => list.composite_taggings %>
<% end if list && !list.composite_taggings.empty? %>