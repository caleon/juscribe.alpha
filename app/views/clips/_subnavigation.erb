<%= subnav_for(@widgetable) %>
<%= bread_el 'Clips', clips_path_from_widgetable(@widgetable), :current => true, :rel => controller.action_name != 'index' ? 'directory' : nil %>
<%= subnavi_el 'New', clip_path_from_widgetable(@widgetable, :prefix => 'new'), :current => true %>