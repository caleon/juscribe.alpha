<%= @widgetable.path_name_prefix %>
<br />
<%= @widgetable.to_path(true).inspect %>
<br />
<% form_for :clip, @clip, :url => clip_path_from_widgetable(@widgetable), :html => { :method => :post } do |f| %>
	<%= f.text_field :name %>
	<%= f.submit %>
<% end %>