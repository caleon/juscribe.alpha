<% form_for @thoughtlet, :url => edit_user_thoughtlet_path(@thoughtlet.to_path), :html => { :method => :put, :multipart => true } do |f| %>
	<%= render :partial => 'form', :locals => { :f => f } %>
	<%= f.submit %>
<% end %>