<%= commenter_for(comment) %> said <%= comment.created_at.recent? ? "#{time_ago_in_words(comment.created_at)} ago" : "on #{comment.created_at.to_s(comment.created_at.this_year? ? :this_year : :not_this_year)}" %>...