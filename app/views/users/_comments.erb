<h3>Comments</h3>
<%= render :partial => 'comments/comment', :collection => user.comments %>
<%= render(:partial => 'comment_form', :locals => { :user => user, :comment => Comment.new }) if logged_in? %>