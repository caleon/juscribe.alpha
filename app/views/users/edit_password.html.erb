<% form_for :user, @user, :url => update_password_user_path, :html => { :method => :put, :class => 'userForm' } do |f| %>
	<%= error_messages_for :user %>
	<%= render :partial => 'password_form', :locals => { :f => f } %>
	<%= submit_tag 'Change', :disable_with => 'Please wait...' %>
<% end %>