<% form_for @user, :html => { :multipart => true } do |f| %>
	<%= error_messages_for :user %>
	<%= render :partial => 'form', :layout => 'new_form', :locals => { :f => f } %>
	<%= submit_tag "Register", :disable_with => "Please wait..." %>
<% end %>