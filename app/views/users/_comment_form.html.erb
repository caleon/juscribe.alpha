<% form_for :comment, @comment, :url => user_comments_path(@user), :html => { :class => 'commentForm', :method => 'post' } do |f| %>
	<fieldset>
		<legend>Comment on <%= @user %></legend>
		<%= render :partial => 'comments/form', :locals => { :f => f } %>
	</fieldset>
<% end %>