<h3>More pictures of <%= picture.depictable.display_name %></h3>
<ul id="<%= picture.depictable.class.class_name.underscore %>-other_pictures" class="media_listing">
<%# The following compact added in case thumbnails dont exist for some reason. %>
<% other_pictures[0..5].map{|pic| pic.thumbnails.find_by_thumbnail('thumb') }.compact.each do |pic| %>
	<li class="pic">
		<%= link_to image_tag(pic.public_filename, :class => dom_class(pic) + (pic == picture ? ' current' : '')), picture_path_for(pic.parent) %>
	</li>
<% end %>
</ul>
<div class="msm_widget-footer">
	<%= link_to 'View all pictures', pictures_path_from_depictable(picture.depictable) %>
	<%# FIXME: the following is for user's depictables..., user_pictures_path() %>
</div>
