<% form_for :blog, @blog, :url => blog_path_for(@blog), :html => { :method => :put, :multipart => true } do |f| %>
	<fieldset>
		<legend>Edit your blog</legend>
		<%= render :partial => 'form', :locals => { :f => f } %>
		<%= f.submit %>
	</fieldset>
<% end %>