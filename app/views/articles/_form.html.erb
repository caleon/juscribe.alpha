<%= f.label :content %>
<%= f.text_area :content %>
<br />
<%= render :partial => 'pictures/form' %>
<br />
<%= f.select :blog_id, @user.blogs.map{|b| [ b.name, b[:id] ] } %>
<br />
<% if @article.new_record? || @article.draft? %>
<input type="submit" name="article[publish]" value="Publish" />
<% end %>

<input type="submit" name="article[publish]" value="Save as Draft" />