<% form_for :article, @article, :url => update_article_path(@article.to_hash), :html => { :class => 'articleForm', :method => :put } do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<br />
	<%= f.label :content %>
	<%= f.text_area :content %>
	<%= submit_tag 'Edit' %>
<% end %>