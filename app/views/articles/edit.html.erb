<% form_for :article, @article, :url => article_path(@article.hash_for_path), :html => { :class => 'articleForm', :method => :put } do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<br />
	<%= f.label :content %>
	<%= f.text_area :content %>
	<%= submit_tag 'Edit' %>
<% end %>