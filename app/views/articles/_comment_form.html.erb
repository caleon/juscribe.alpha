<% form_for :comment, @comment, :url => '#', :html => { :class => 'commentForm', :method => 'post' } do |f| %>
	<fieldset>
		<legend>Comment on this article</legend>
		<%= render :partial => 'comments/form', :locals => { :f => f } %>
	</fieldset>
<% end %>