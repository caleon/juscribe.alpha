<% form_for :comment, comment, :url => article_comments_path(article.to_path(true)), :html => { :class => 'commentForm', :method => 'post' } do |f| %>
	<fieldset>
		<legend>Comment on this article</legend>
		<%= render :partial => 'comments/form', :locals => { :f => f } %>
	</fieldset>
<% end %>