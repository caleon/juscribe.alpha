<div class="sub navigation">
	<ul class="navigationList">
		<%= navi_el @user.nick, @user %>
<% if @user && !@user.new_record? %>
	<%= navi_el "Articles", articles_path(@user) %>
	<% if logged_in? && this_is_viewer? %>
		<%= navi_el "New Article...", new_article_path(@user) %>
	<% end %>
<% end %>
	<%= navi_skin_info %>
	</ul>
</div>
