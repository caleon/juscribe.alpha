<% form_for @article, :html => { :class => 'articleForm', :method => :post, :multipart => true } do |f| %>
	<fieldset>
		<%= f.label :title %>
		<%= f.text_field :title %>
		<br />
		<%= render :partial => 'form', :locals => { :f => f } %>
	</fieldset>
<% end %>