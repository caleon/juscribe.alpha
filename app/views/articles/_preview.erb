<div id="preview_pane-inner">
	<% if preview ||= nil %>
		<%= picture_for(preview) %>
		<h4><%= preview.title %></h4>
		<%= format_article(preview, :truncate => 200, :without_intro => true) %>
		<%= byline_for(preview) %>
	<% else %>
		Nothing selected
	<% end %>
</div>