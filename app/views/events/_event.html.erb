<li class="event">
	<span>
		<%= link_to event.name, user_event_path(event.user, event) %> 
	<% if event.begins_at %>
		<span class="timestamp"><%= event.begins_at.to_s(:concise) %></span>
	<% end %>
	</span>
</li>
