<% form_for :event, @event, :url => user_event_path(@event.user, @event), :html => { :class => "eventForm", :method => "post" } do |f| %>
	<fieldset>
		<legend>New Event</legend>
		<%= render :partial => 'form', :locals => { :f => f } %>
		<%= submit_tag "Create" %>
	</fieldset>
<% end %>